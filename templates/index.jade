doctype html
html
  body
    div.categories
      each category in categories
        ul
          li
            | name: #{category.name}
          li
            | description: #{category.description}
          li
            | link: #{category.link}
